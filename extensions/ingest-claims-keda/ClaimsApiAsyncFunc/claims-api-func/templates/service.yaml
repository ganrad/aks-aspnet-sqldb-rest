apiVersion: v1
kind: Service
metadata:
  name: {{ template "claims-api-func.fullname" . }}
  labels:
    app: {{ template "claims-api-func.name" . }}
    chart: {{ template "claims-api-func.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "claims-api-func.name" . }}
    release: {{ .Release.Name }}
