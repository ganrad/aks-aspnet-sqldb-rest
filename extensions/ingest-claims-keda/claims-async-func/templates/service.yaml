apiVersion: v1
kind: Service
metadata:
  name: {{ template "claims-async-func.fullname" . }}
  labels:
    app: {{ template "claims-async-func.name" . }}
    chart: {{ template "claims-async-func.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "claims-async-func.name" . }}
    release: {{ .Release.Name }}
